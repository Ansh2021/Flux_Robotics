import express, { Request, Response } from "express";

const router = express.Router();

//TODO: replace the req/res with the controller i will be using for this
//almost done
router.post("/wordle", async (req: Request, res: Response) => {});
